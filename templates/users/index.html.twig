{% extends 'base.html.twig' %}

{% block title %}Liste des Events - Symphonia{% endblock %}

{% block body %}
    <div>
        <h1>Liste des users</h1>
        {% if users is empty %}
            <p>Aucun users trouvé.</p>
        {% else %}
            <ul>
                {% for user in users %}
                    <a href="{{ path('app_users_details', {'id': user.id}) }}">{{ user.name }}</a>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
{% endblock %}